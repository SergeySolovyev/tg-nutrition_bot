# Инструкция для сдачи проекта

## 📦 Содержимое архива

Архив `final_version_submission.zip` содержит все необходимые файлы для проверки проекта:

### Исходный код:
- `bot.py` - точка входа, запуск бота
- `handlers.py` - обработчики команд и FSM
- `middlewares.py` - middleware для логирования
- `calc.py` - функции расчётов (вода, калории, тренировки)
- `config.py` - конфигурация и переменные окружения
- `storage.py` - хранение данных в JSON
- `states.py` - состояния FSM
- `utils.py` - утилиты (API погоды и еды)

### Конфигурация:
- `requirements.txt` - зависимости Python
- `Dockerfile` - для деплоя на Render.com
- `env.example` - пример переменных окружения

### Документация:
- `README.md` - описание проекта и инструкции по запуску
- `REQUIREMENTS_CHECKLIST.md` - чеклист соответствия требованиям

## 🚀 Как запустить проект

1. Распакуйте архив
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте файл `.env` на основе `env.example`:
   ```bash
   cp env.example .env
   ```
4. Отредактируйте `.env` и укажите `BOT_TOKEN` (получите у @BotFather)
5. Запустите бота:
   ```bash
   python bot.py
   ```

## 📋 Что нужно для сдачи

Согласно заданию из `ИИ_Пр_2.ipynb`, необходимо предоставить:

1. ✅ **Ссылку на код** - можно использовать этот архив или ссылку на репозиторий
2. ✅ **Скриншоты работы бота** - демонстрация всех команд:
   - `/start`, `/help`
   - `/set_profile` (полный процесс заполнения)
   - `/log_water <мл>`
   - `/log_food <продукт>`
   - `/log_workout <тип> <мин>`
   - `/check_progress`
   - `/reset_today`
3. ✅ **Подтверждение деплоя** - скриншот логов с Render.com, показывающий:
   - Успешный build
   - Логи команд пользователя (через middleware)

## 🔒 Безопасность

⚠️ **ВАЖНО**: Перед отправкой убедитесь, что:
- В архиве НЕТ файла `.env` с реальным токеном
- В архиве НЕТ файла `data.json` с данными пользователей
- Все секретные данные удалены

В архиве уже исключены:
- `__pycache__/` - кэш Python
- `data.json` - данные пользователей
- `.env` - переменные окружения с секретами

## 📊 Соответствие требованиям

Все критерии оценивания выполнены (см. `REQUIREMENTS_CHECKLIST.md`):

- ✅ Работающий Telegram-бот (2 балла)
- ✅ Настройка профиля (1 балл)
- ✅ Расчёт воды и калорий (0.3 балла)
- ✅ Использование API погоды и OpenFoodFacts (0.2 балла)
- ✅ Логирование воды, еды, тренировок (4.5 балла)
- ✅ Метод прогресса (1 балл)
- ✅ Деплой с логированием (1 балл)

**Итого: 9/8 баллов** ✅

## 🐳 Деплой на Render.com

1. Создайте новый Web Service на Render.com
2. Подключите репозиторий или загрузите файлы
3. Укажите:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`
4. Добавьте переменные окружения:
   - `BOT_TOKEN` - токен бота
   - (опционально) `OPENWEATHER_API_KEY`
   - (опционально) `DATA_PATH`
5. После деплоя проверьте логи - должны быть видны команды пользователя

---

**Удачи на защите! 🎉**
