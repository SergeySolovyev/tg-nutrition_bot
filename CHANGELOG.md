# Changelog - Финальные исправления перед сдачей

## Исправления от 16.01.2026

### ✅ Исправлено:

1. **Очищен data.json** - теперь содержит только пустой шаблон `{"users": {}}`
   - Удалены все реальные данные пользователей
   - Файл будет создаваться автоматически при первом запуске

2. **Улучшена формула расчёта воды за активность**
   - Было: пропорционально `(activity_min / 30.0) * 500.0`
   - Стало: дискретно `(activity_min // 30) * 500.0`
   - Теперь учитываются только **полные блоки по 30 минут**
   - Пример: 45 минут активности = 1 блок = +500 мл (а не 750 мл)

3. **Улучшен Dockerfile**
   - Добавлен флаг `--no-cache-dir` для уменьшения размера образа
   - `RUN pip install --no-cache-dir -r requirements.txt`

4. **Удалён __pycache__**
   - Кэш Python удалён из проекта
   - Добавлен в `.gitignore` для предотвращения попадания в репозиторий

5. **Обновлён .gitignore**
   - Исключает `__pycache__/`, `*.pyc`, `data.json`, `.env`
   - Предотвращает попадание лишних файлов в архив

### 📦 Архив для сдачи:

- **Имя:** `final_version_submission.zip`
- **Расположение:** корневая директория проекта
- **Содержит:** 15 файлов (все исходники, конфигурация, документация)
- **Исключено:** `__pycache__/`, `data.json`, `.env`

### ✅ Проект готов к сдаче!

Все требования выполнены, код чистый, архив подготовлен.
