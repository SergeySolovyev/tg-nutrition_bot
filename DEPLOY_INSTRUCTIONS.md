# 🚀 Инструкция по деплою на Render.com через GitHub

## Шаг 1: Загрузка кода на GitHub

### Вариант A: Через командную строку (рекомендуется)

1. **Инициализируйте git репозиторий** (если еще не сделано):
```bash
cd final_version
git init
```

2. **Добавьте удаленный репозиторий**:
```bash
git remote add origin https://github.com/SergeySolovyev/tg-nutrition_bot.git
```

3. **Добавьте все файлы**:
```bash
git add .
```

4. **Сделайте первый коммит**:
```bash
git commit -m "Initial commit: Telegram nutrition bot with advanced features"
```

5. **Загрузите на GitHub**:
```bash
git branch -M main
git push -u origin main
```

### Вариант B: Через GitHub Desktop или веб-интерфейс

1. Откройте https://github.com/SergeySolovyev/tg-nutrition_bot
2. Нажмите "uploading an existing file"
3. Перетащите все файлы из папки `final_version` (кроме тех, что в .gitignore)
4. Сделайте коммит с сообщением "Initial commit"

## Шаг 2: Настройка Render.com

1. **Зайдите на https://render.com** и войдите в аккаунт

2. **Создайте новый Web Service**:
   - Нажмите "New" → "Web Service"
   - Выберите репозиторий `SergeySolovyev/tg-nutrition_bot`
   - Или подключите через "Public Git repository" → вставьте URL репозитория

3. **Настройте параметры**:
   - **Name**: `tg-nutrition-bot` (или любое другое имя)
   - **Environment**: `Python 3`
   - **Build Command**: оставьте пустым (Render использует Dockerfile автоматически)
   - **Start Command**: `python bot.py`
   - **Plan**: выберите Free план (достаточно для бота)

4. **Добавьте переменные окружения** (Environment Variables):
   - Нажмите "Add Environment Variable"
   - Добавьте:
     ```
     BOT_TOKEN = ваш_токен_бота_от_BotFather
     DATA_PATH = data.json
     ```
   - Опционально:
     ```
     OPENWEATHER_API_KEY = ваш_ключ (если есть)
     ```

5. **Сохраните и деплойте**:
   - Нажмите "Create Web Service"
   - Render автоматически начнет сборку и деплой

## Шаг 3: Проверка деплоя

1. **Проверьте логи**:
   - В разделе "Logs" должны быть видны:
     ```
     Бот запущен!
     ```
   - Если есть ошибки - проверьте переменные окружения

2. **Протестируйте бота**:
   - Откройте Telegram и найдите вашего бота
   - Отправьте `/start`
   - В логах Render должно появиться:
     ```
     User 123456 (@username): /start
     ```

3. **Сделайте скриншот логов**:
   - Для сдачи задания нужен скриншот, показывающий:
     - ✅ Успешный build
     - ✅ Сообщение "Бот запущен!"
     - ✅ Логи команд пользователя (через middleware)

## ⚠️ Важные замечания

1. **Не загружайте секреты в GitHub**:
   - ✅ `.env` файл уже в `.gitignore` - не будет загружен
   - ✅ `data.json` тоже в `.gitignore`
   - ✅ Все секреты только через Environment Variables в Render

2. **Если бот не запускается**:
   - Проверьте, что `BOT_TOKEN` правильно установлен в Render
   - Проверьте логи на ошибки
   - Убедитесь, что все зависимости в `requirements.txt`

3. **Для обновления кода**:
   - Сделайте изменения локально
   - `git add .`
   - `git commit -m "описание изменений"`
   - `git push`
   - Render автоматически пересоберет и задеплоит

## 📋 Чеклист перед деплоем

- [ ] Код загружен на GitHub
- [ ] `.env` файл НЕ загружен (проверьте .gitignore)
- [ ] `data.json` НЕ загружен
- [ ] Все файлы проекта на месте
- [ ] `Dockerfile` присутствует
- [ ] `requirements.txt` содержит все зависимости
- [ ] `BOT_TOKEN` готов для добавления в Render

## 🎉 Готово!

После успешного деплоя ваш бот будет работать 24/7 на Render.com!
