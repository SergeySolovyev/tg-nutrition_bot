# Telegram Bot Token (обязательно)
# Получите токен у @BotFather в Telegram
BOT_TOKEN=your_bot_token_here

# Путь к файлу с данными пользователей (опционально, по умолчанию: data.json)
DATA_PATH=data.json

# OpenWeatherMap API Key (опционально)
# Если не указан, используется бесплатный Open-Meteo API
OPENWEATHER_API_KEY=
